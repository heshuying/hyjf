package com.hyjf.test.act;

public class NewYearTest {
/*
    @Test
    public void testGetUserCardCount() {
        Map<String, String> params = new HashMap<String, String>();
        String userId = "22400192";
        String timestamp = "1436254211";
        String accessKey = PropUtils.getSystem("aop.interface.accesskey");
        String sign = MD5.toMD5Code(accessKey + userId + timestamp + accessKey);

        params.put("userId", userId);
        params.put("timestamp", timestamp);
        params.put("chkValue", sign);

        String loginUrl = "http://localhost:8080/hyjf-api-web/wealthcard/getUserCardCount.json";

        String result = HttpClientUtils.post(loginUrl, params);

        System.out.println(result);
    }

    @Test
    public void testPhoneNumCheck() {
        Map<String, String> params = new HashMap<String, String>();
        String phoneNum = "17688541245";
        String timestamp = "1436254211";
        String accessKey = PropUtils.getSystem("aop.interface.accesskey");
        String sign = MD5.toMD5Code(accessKey + phoneNum + timestamp + accessKey);

        params.put("phoneNum", phoneNum);
        params.put("timestamp", timestamp);
        params.put("chkValue", sign);

        String loginUrl = "http://localhost:8080/hyjf-api-web/wealthcard/doPhoneNumCheck.json";

        String result = HttpClientUtils.post(loginUrl, params);

        System.out.println(result);
    }

    @Test
    public void testCardSend() {
        Map<String, String> params = new HashMap<String, String>();
        String userId = "22400192";
        String phoneNum = "17889666998";
        String cardIdentifier = "4";
        String timestamp = "1484212911";
        String accessKey = PropUtils.getSystem("aop.interface.accesskey");
        String sign = MD5.toMD5Code(accessKey + userId + phoneNum + cardIdentifier + timestamp + accessKey);

        params.put("userId", userId);
        params.put("phoneNum", phoneNum);
        params.put("cardIdentifier", cardIdentifier);
        params.put("timestamp", timestamp);
        params.put("chkValue", sign);

        String loginUrl = "http://localhost:8080/hyjf-api-web/wealthcard/doCardSend.json";

        String result = HttpClientUtils.post(loginUrl, params);

        System.out.println(result);
    }

    @Test
    public void testNewYearPrizeDraw() {
        Map<String, String> params = new HashMap<String, String>();
        String userId = "22400192";
        String timestamp = "1436254211";
        String accessKey = PropUtils.getSystem("aop.interface.accesskey");
        String sign = MD5.toMD5Code(accessKey + userId + timestamp + accessKey);

        params.put("userId", userId);
        params.put("timestamp", timestamp);
        params.put("chkValue", sign);

        String loginUrl = "http://localhost:8080/hyjf-api-web/wealthcard/getUserCardCount.json";

        String result = HttpClientUtils.post(loginUrl, params);

        System.out.println(result);
    }*/

}
